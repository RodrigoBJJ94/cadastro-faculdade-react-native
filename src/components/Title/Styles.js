import { StyleSheet } from "react-native";

export default StyleSheet.create({
    viewTitle: {
        alignItems: "center",
        marginTop: 20,
        marginBottom: 20,
    },
    title: {
        fontSize: 30,
        fontWeight: "bold",
        color: "#000000",
    }
});
